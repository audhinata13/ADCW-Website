<label class="pr-3">
    <input type="checkbox" name="{{ $name }}" class=" rounded-md border  {{ $class }}"
        value="{{ $value }}" id="{{ $id }}" {{ $checked ? 'checked' : '' }}>
    {{ $label }} &nbsp;&nbsp;
</label>
